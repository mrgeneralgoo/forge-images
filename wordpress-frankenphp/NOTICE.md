This image contains WordPress core copied from the pinned official WordPress
image into a pinned FrankenPHP runtime.

The upstream license is copied during the image build to
`/usr/share/doc/wordpress/LICENSE`; the source reference is recorded in
`/usr/share/doc/wordpress/PROVENANCE`.

No application extensions, themes, configuration, secret material, or
deployment data are part of this image.
